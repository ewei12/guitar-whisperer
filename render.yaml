services:
  - type: web
    name: tab-backend
    env: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python worker.py & gunicorn -w 1 --threads 4 -b 0.0.0.0:$PORT app:app"
    plan: free
