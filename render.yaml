services:
  - type: web
    name: tab-backend
    env: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 1 -k sync -b 0.0.0.0:$PORT --timeout 300 app:app"
    plan: free
